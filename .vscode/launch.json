{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "express-app",
      "program": "${workspaceFolder}/src/server.js",
      "envFile": "${workspaceRoot}/.vscode/.env"
    }
  ]
}